package utiles;

import com.badlogic.gdx.Gdx;

public class Constants {
		public final static float PIXELS_TO_METERS = 0.01f;
		public final static float GROUND_START = 1.935f;
		public final static float METERS_TO_PIXELS = 1 / PIXELS_TO_METERS;
		public final static float INIX = (Gdx.graphics.getWidth()/2)*PIXELS_TO_METERS;
}
